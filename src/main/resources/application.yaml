spring:
  datasource:
    username: root
    password: belton
    url: jdbc:mysql://localhost:3306/mineServer
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format-sql: true
    show_sql: true
